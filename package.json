{
  "name": "audd-subscriptions",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web && npm run build --workspace=@audd/widget",
    "lint": "npm run lint --workspace=web"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
